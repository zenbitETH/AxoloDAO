# macOS
.DS_Store
.AppleDouble
.LSOverride

# Node / Astro site
node_modules/
dist/
.astro/
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
Thumbs.db

# Local-only Claude tooling (not for public repo)
.claude/
CLAUDE.local.md

# Local-only operational data (xlsx exports, working files)
Context/

# Private water-telemetry seed kit (input to a separate private repo)
water-telemetry-seed/

# Local-only Claude entrypoint (content lives in private axolodao-brain repo)
CLAUDE.local.md

# Scratch/debug scripts (leading underscore) — never publish: they hardcode
# absolute paths into the private operations workbook.
Site/scripts/_*.mjs
